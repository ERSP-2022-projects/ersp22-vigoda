#Nexus
begin mrbayes;
    execute <input_filename>;
    lset nst=<nst> rates=<rates>;
    mcmc ngen=<ngen> samplefreq=<samplefreq> nchains=<nchains> nruns=<nruns> printfreq=<printfreq> diagnfreq=<diagnfreq>;
    sumt burnin=<burnin> relburnin=yes;
    sump burnin=<burnin> relburnin=yes standarddeviation=yes;
    log likelihood;
    log <log_filename>;
    set autoclose=yes nowarn=yes;
    quit;
end;
